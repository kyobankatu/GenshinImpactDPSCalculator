/**
 * Sample entry points are allowed to contain simulator bootstrap wiring and
 * human-readable setup code.
 *
 * <p>They may depend on display names when scripting scenarios, but that
 * translation should not leak back into runtime or analysis packages.
 */
package sample;

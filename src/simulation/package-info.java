/**
 * The simulation runtime should not depend on profile-file strings or
 * report-specific labels for core control flow.
 *
 * <p>String-based entry points may remain as compatibility adapters, but typed
 * identifiers are the primary runtime API.
 */
package simulation;

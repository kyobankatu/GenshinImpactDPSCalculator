package model.entity;

import simulation.CombatSimulator;

/**
 * Marker interface for characters with reaction-listener behavior.
 */
public interface ReactionAwareCharacter extends CombatSimulator.ReactionListener {
}

package simulation.action;

/** Typed activation mode for character Skills with Press and Hold variants. */
public enum SkillActionMode {
    /** Default short Skill activation. */
    PRESS,
    /** Explicit held Skill activation. */
    HOLD
}
